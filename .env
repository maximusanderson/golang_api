# Server Configuration
PORT=8080

# Database Configuration
# Use your Dokploy PostgreSQL connection details
DB_HOST=mynew-postgres-jgcyi2
DB_PORT=5432
DB_USER=db-user
DB_PASSWORD=db-password
DB_NAME=db-name
DB_SSLMODE=disable

# SuperTokens Configuration
# If running locally and SuperTokens is on Dokploy, use the service name or external IP
# For local development, you might need to use localhost:3567 if port is forwarded
# For production deployment on Dokploy, use: http://supertokens:3567
SUPERTOKENS_CONNECTION_URI=http://localhost:3567
SUPERTOKENS_API_KEY=
